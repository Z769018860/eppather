#include <cstdio>
using namespace std;

int t;
char d;
int main(){
    freopen("copycat.in", "r", stdin);
    freopen("copycat.out", "w", stdout);
    scanf("%d\n", &t);
    while ((d = getchar()) != EOF) putchar(d);
    return 0;
}