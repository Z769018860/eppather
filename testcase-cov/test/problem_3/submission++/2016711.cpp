#include <bits/stdc++.h>

using namespace std;

int main()
{
    freopen("copycat.in", "r", stdin);
    freopen("copycat.out", "w", stdout);

    int n;
    string str;

    cin >> n;
    while (n -- )
    {
        cin >> str;

        cout << str << endl;
    }
    return 0;
}