/* Generated auto-compatibility summary model for eppather summary mode. */

/* project=sds EPPATHER_ENTRY=sdscatsds slice=auto_compat */

int *sdscatsds(int *s, int *t)
{
    int mem = 0;
    if (s) {
        mem = mem + s[0];
    }
    if (t) {
        mem = mem + t[0];
    }
    return 0;
}


