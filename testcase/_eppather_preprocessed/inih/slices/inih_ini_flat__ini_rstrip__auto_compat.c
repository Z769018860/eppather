/* Generated auto-compatibility summary model for eppather summary mode. */

/* project=inih EPPATHER_ENTRY=ini_rstrip slice=auto_compat */

int *ini_rstrip(int *s, int *end)
{
    int mem = 0;
    if (s) {
        mem = mem + s[0];
    }
    if (end) {
        mem = mem + end[0];
    }
    return 0;
}


