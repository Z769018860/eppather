int func(int arSize_param) {
    int ar[5];
    int p[5];
    int arSize;
    arSize = arSize_param;
    ar[0] = 1;
    
    for (p[0] = 0; p[0] < arSize; p[0] = p[0] + 1) {
        int temp;
        temp = ar[p[0]];
    }
    
    return;
}
