int arr[5];
int f;
int t;
int i;
struct Node* e;
struct Node* listNew(int size, int* data);

for (i = f + 1; i <= t; i = i + 1) {
    arr[0] = sizeof(i);
    e = e->nx = listNew(arr[0], &i);
}
return;
