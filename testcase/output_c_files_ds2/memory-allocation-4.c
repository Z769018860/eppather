int funcB_funcC(int a, int n1, int n2)
{
    int integers[5];
    int ints[5];
    int i;
    for (i = 0; i < 5; i = i + 1)
    {
        integers[i] = 0;
        ints[i] = 0;
    }
    integers[0] = a;
    return integers[0] + ints[0];
    return;
}
