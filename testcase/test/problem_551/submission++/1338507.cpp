#include<bits/stdc++.h>
using namespace std;

int main(){int T; long long N; for(cin >> T ; T ; --T){cin >> N; cout << (N >= 4 ? "Yes" : "No") << endl << (N >= 4 && N % 2 == 0 ? "Yes" : "No") << endl;} return 0;}
