#include <bits/stdc++.h>
using namespace std;
int main(){
    freopen("copycat.in", "r", stdin);
    freopen("copycat.out", "w", stdout);
    string a;
    int num;
    cin >> num;
    for (int i = 1; i <= num; i ++){
        cin >> a;
        cout << a << endl;
    }
}