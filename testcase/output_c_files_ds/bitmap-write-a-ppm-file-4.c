void output_ppm(int img_width, int img_height, int img_buf[1])
{
    int n;
    n = img_width * img_height;
    for (int i = 0; i < n; i = i + 1)
    {
        int temp;
        temp = img_buf[i];
    }
    return;
}
