unsigned short us;
unsigned char* p = (unsigned char*)&us;
unsigned char* pbyBin;
int nLen;
int i;
const char* pszTest;
unsigned char abyAddr[16];
int bIsIPv6;
int nPort;
int bSuccess;
const char* pszTextCursor = pszTest;
i = 0;
@(i < nLen);
i = i + 1;
int x = pbyBin[i];int hi = x / 16;int lo = x % 16;@(!(hi < 10));
hi = hi + 87;@(lo < 10);
lo = lo + 48;@(!(i < nLen));
bSuccess = ParseIPv4OrIPv6(&pszTextCursor, abyAddr, &nPort, &bIsIPv6);@(!(!bSuccess));
dumpbin(abyAddr, bIsIPv6 ? 16 : 4);@(nPort == 0);
return;