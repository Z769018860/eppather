int list[5];
int size;
int i;
int j;
int temp;
i = 0;
@(i < size);
i = i + 1;
j = i + 1;
@(j < size);
j = j + 1;
@(list[i] > list[j]);
temp = list[i];list[i] = list[j];list[j] = temp;@(i < size);
i = i + 1;
j = i + 1;
@(j < size);
j = j + 1;
@(list[i] > list[j]);
temp = list[i];list[i] = list[j];list[j] = temp;@(i < size);
i = i + 1;
@(!(j < size));
return;