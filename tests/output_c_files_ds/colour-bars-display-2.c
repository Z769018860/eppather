void draw_colors(int maxX, int maxY, int maxColours) {
    int i;
    for (i = 0; i < maxColours; i = i + 1) {
        if (1) {
            int x1 = i * maxX / maxColours;
            int x2 = (i + 1) * maxX / maxColours;
        }
    }
    return;
}
