extern const char *SDS_NOINIT;
const char *SDS_NOINIT = "SDS_NOINIT";
const char *from;
const char *to;
size_t setlen;
j = 0;
@(j < l);
j++;
i = 0;
@(i < setlen);
i++;
@(s[j] == from[i]);
s[j] = to[i];
break;
@(j < l);
j++;
i = 0;
@(i < setlen);
i++;
@(!(s[j] == from[i]));
@(i < setlen);
i++;
@(s[j] == from[i]);
s[j] = to[i];
break;
@(!(j < l));
return s;
