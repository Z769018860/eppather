typedef long long _64;
_64 prefix[1000001];
_64 n;
_64 x;
int place;
_64 variant;
int place;
_64 ans=0;
int left;
int right;
_64 variant;
_64 m,p;
int i=place;
@(i<=n);
i+=bit2(i);
prefix[i]+=variant;
@(i<=n);
i+=bit2(i);
prefix[i]+=variant;
@(i<=n);
i+=bit2(i);
prefix[i]+=variant;
@(!(i<=n));
@(place);
ans+=prefix[place];
place-=bit2(place);
@(place);
ans+=prefix[place];
place-=bit2(place);
@(place);
ans+=prefix[place];
place-=bit2(place);
@(!(place));

return ans;
