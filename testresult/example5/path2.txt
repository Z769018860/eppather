int arr[10];
int size;
int incLength = 1, decLength = 1;
int i;
i = 1;
@(i < size);
i = i + 1;
@(arr[i] > arr[i - 1]);
incLength = incLength + 1;

@(i < size);
i = i + 1;
@(!(arr[i] > arr[i - 1]));
break;

@(!(i < size));
@(!(i < size));
@(incLength > 1 && decLength > 1);
;
;
