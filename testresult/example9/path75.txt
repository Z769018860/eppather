int arr[5];
int size;
int count = 0;
@(!(size < 2));


int i = 1;
@(i < size - 1);
i=i+1;
@(!(i % 2 != 0));


@(i % 2 == 0);
@(!(arr[i] % 2 != 0));


;

@(i < size - 1);
i=i+1;
@(i % 2 != 0);
@(arr[i] % 2 == 0);
@(arr[i] < arr[i - 1]);
@(arr[i] != 0);
count=count+1;

;

;

;

@(i % 2 == 0);
@(!(arr[i] % 2 != 0));


;

@(!(i < size - 1));
return count;
