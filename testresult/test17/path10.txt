int x;
int v[5];
int n;
int low,high,mid;
low=0;
high=n-1;
@(low<=high);
mid=(high+low)/2;
@(!(x>v[mid]));

@(x<v[mid]);
high=mid-1;


@(low<=high);
mid=(high+low)/2;
@(!(x>v[mid]));

@(x<v[mid]);
high=mid-1;


@(low<=high);
mid=(high+low)/2;
@(x>v[mid]);
low=mid+1;

@(!(low<=high));

return -1;
