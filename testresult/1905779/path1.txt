const unsigned M = 1000000007;
void Input(long a[500][500], int m, int n);
void Calc(long a[500][500], long b[500][500], long c[500][500], int n, int m, int p);
void Output(long c[500][500], int n, int m);
int n, p, m;
long a[500][500], b[500][500], c[500][500];
long a[500][500];
int m;
int n;
int i, j;
long a[500][500];
long b[500][500];
long c[500][500];
int n;
int m;
int p;
int i, j, k;
long c[500][500];
int n;
int m;
int i, j;
i = 0;
@(i < m);
i++;
j = 0;
@(j < n);
j++;
scanf("%ld", &a[i][j]);
@(j < n);
j++;
scanf("%ld", &a[i][j]);
@(j < n);
j++;
scanf("%ld", &a[i][j]);
@(!(j < n));
@(i < m);
i++;
j = 0;
@(j < n);
j++;
scanf("%ld", &a[i][j]);
@(j < n);
j++;
scanf("%ld", &a[i][j]);
@(j < n);
j++;
scanf("%ld", &a[i][j]);
@(!(j < n));
@(i < m);
i++;
j = 0;
@(j < n);
j++;
scanf("%ld", &a[i][j]);
@(j < n);
j++;
scanf("%ld", &a[i][j]);
@(j < n);
j++;
scanf("%ld", &a[i][j]);
@(!(j < n));
@(!(i < m));
i = 0;
@(i < n);
i++;
j = 0;
@(j < m);
j++;
long long sum = 0;
k = 0;
@(k < p);
k++;
sum += (((a[i][k] + M) % M) * ((b[k][j] + M) % M)) % M;
@(k < p);
k++;
sum += (((a[i][k] + M) % M) * ((b[k][j] + M) % M)) % M;
@(k < p);
k++;
sum += (((a[i][k] + M) % M) * ((b[k][j] + M) % M)) % M;
@(!(k < p));
c[i][j] = sum;
@(j < m);
j++;
long long sum = 0;
k = 0;
@(k < p);
k++;
sum += (((a[i][k] + M) % M) * ((b[k][j] + M) % M)) % M;
@(k < p);
k++;
sum += (((a[i][k] + M) % M) * ((b[k][j] + M) % M)) % M;
@(k < p);
k++;
sum += (((a[i][k] + M) % M) * ((b[k][j] + M) % M)) % M;
@(!(k < p));
c[i][j] = sum;
@(j < m);
j++;
long long sum = 0;
k = 0;
@(k < p);
k++;
sum += (((a[i][k] + M) % M) * ((b[k][j] + M) % M)) % M;
@(k < p);
k++;
sum += (((a[i][k] + M) % M) * ((b[k][j] + M) % M)) % M;
@(k < p);
k++;
sum += (((a[i][k] + M) % M) * ((b[k][j] + M) % M)) % M;
@(!(k < p));
c[i][j] = sum;
@(!(j < m));
@(i < n);
i++;
j = 0;
@(j < m);
j++;
long long sum = 0;
k = 0;
@(k < p);
k++;
sum += (((a[i][k] + M) % M) * ((b[k][j] + M) % M)) % M;
@(k < p);
k++;
sum += (((a[i][k] + M) % M) * ((b[k][j] + M) % M)) % M;
@(k < p);
k++;
sum += (((a[i][k] + M) % M) * ((b[k][j] + M) % M)) % M;
@(!(k < p));
c[i][j] = sum;
@(j < m);
j++;
long long sum = 0;
k = 0;
@(k < p);
k++;
sum += (((a[i][k] + M) % M) * ((b[k][j] + M) % M)) % M;
@(k < p);
k++;
sum += (((a[i][k] + M) % M) * ((b[k][j] + M) % M)) % M;
@(k < p);
k++;
sum += (((a[i][k] + M) % M) * ((b[k][j] + M) % M)) % M;
@(!(k < p));
c[i][j] = sum;
@(j < m);
j++;
long long sum = 0;
k = 0;
@(k < p);
k++;
sum += (((a[i][k] + M) % M) * ((b[k][j] + M) % M)) % M;
@(k < p);
k++;
sum += (((a[i][k] + M) % M) * ((b[k][j] + M) % M)) % M;
@(k < p);
k++;
sum += (((a[i][k] + M) % M) * ((b[k][j] + M) % M)) % M;
@(!(k < p));
c[i][j] = sum;
@(!(j < m));
@(i < n);
i++;
j = 0;
@(j < m);
j++;
long long sum = 0;
k = 0;
@(k < p);
k++;
sum += (((a[i][k] + M) % M) * ((b[k][j] + M) % M)) % M;
@(k < p);
k++;
sum += (((a[i][k] + M) % M) * ((b[k][j] + M) % M)) % M;
@(k < p);
k++;
sum += (((a[i][k] + M) % M) * ((b[k][j] + M) % M)) % M;
@(!(k < p));
c[i][j] = sum;
@(j < m);
j++;
long long sum = 0;
k = 0;
@(k < p);
k++;
sum += (((a[i][k] + M) % M) * ((b[k][j] + M) % M)) % M;
@(k < p);
k++;
sum += (((a[i][k] + M) % M) * ((b[k][j] + M) % M)) % M;
@(k < p);
k++;
sum += (((a[i][k] + M) % M) * ((b[k][j] + M) % M)) % M;
@(!(k < p));
c[i][j] = sum;
@(j < m);
j++;
long long sum = 0;
k = 0;
@(k < p);
k++;
sum += (((a[i][k] + M) % M) * ((b[k][j] + M) % M)) % M;
@(k < p);
k++;
sum += (((a[i][k] + M) % M) * ((b[k][j] + M) % M)) % M;
@(k < p);
k++;
sum += (((a[i][k] + M) % M) * ((b[k][j] + M) % M)) % M;
@(!(k < p));
c[i][j] = sum;
@(!(j < m));
@(!(i < n));
i = 0;
@(i < n);
i++;
j = 0;
@(j < m);
j++;
printf("%ld ", (c[i][j] + M) % M);
@(j < m);
j++;
printf("%ld ", (c[i][j] + M) % M);
@(j < m);
j++;
printf("%ld ", (c[i][j] + M) % M);
@(!(j < m));
printf("\n");
