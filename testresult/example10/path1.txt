int s[5];
int index[1];
int i = 0;
int c;
@(s[index[0]] == ' ' || s[index[0]] == '\t');
index[0] = index[0] + 1;
@(s[index[0]] == ' ' || s[index[0]] == '\t');
index[0] = index[0] + 1;
@(!(s[index[0]] == ' ' || s[index[0]] == '\t'));

c = s[index[0]];
@(!(c != '.' && (c < '0' || c > '9')));


@(c >= '0' && c <= '9');
s[i] = c;
i = i + 1;
index[0] = index[0] + 1;
@(s[index[0]] >= '0' && s[index[0]] <= '9');
s[i] = s[index[0]];
i = i + 1;
index[0] = index[0] + 1;
@(s[index[0]] >= '0' && s[index[0]] <= '9');
s[i] = s[index[0]];
i = i + 1;
index[0] = index[0] + 1;
@(!(s[index[0]] >= '0' && s[index[0]] <= '9'));

;

@(s[index[0]] == '.');
s[i] = '.';
i = i + 1;
index[0] = index[0] + 1;
@(!(s[index[0]] >= '0' && s[index[0]] <= '9'));

;

s[i] = '\0';
return 48;
