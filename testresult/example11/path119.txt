int arr[5];
int size;
int result = 0;
int sumPositive = 0;
int sumNegative = 0;
int i = 0;
@(i < size);
i = i + 1;
@(arr[i] > 0);
sumPositive = sumPositive + arr[i];
@(arr[i] % 2 == 0);
result = result + arr[i] * 2;

@(i > 0 && arr[i] > arr[i - 1]);
result = result + arr[i] - arr[i - 1];

@(!(arr[i] < 0));


@(!(i > 0 && i < size - 1 && arr[i] > arr[i - 1] + arr[i + 1]));


;

@(i < size);
i = i + 1;
@(arr[i] > 0);
sumPositive = sumPositive + arr[i];
@(arr[i] % 2 == 0);
result = result + arr[i] * 2;

@(!(i > 0 && arr[i] > arr[i - 1]));


@(arr[i] < 0);
sumNegative = sumNegative + arr[i];
@(arr[i] % 2 != 0);
result = result + arr[i] * 3;

@(i < size - 1 && arr[i] < arr[i + 1]);
result = result - arr[i];

;

@(i > 0 && i < size - 1 && arr[i] > arr[i - 1] + arr[i + 1]);
result = result + arr[i] * 5;

;

@(!(i < size));
@(!(sumPositive > -sumNegative));
result = result + sumNegative;

return result;
