int *a;
int *b;
int size;
int i;
i = 0;
@(i < size);
i = i + 1;
@(a[i] != b[i]);
@(!(a[i] > b[i]));

@(a[i] < b[i]);
a[i] = 0;
b[i] = 0;


;
@(a[i] != b[i]);
@(a[i] > b[i]);
@(!(a[i] < 10 && b[i] < 10));


;

;
@(!(a[i] != b[i]));

;
