char regex[110], str[110];
@(~scanf("%s%s", regex, str));
regex_t reg;
regcomp(&reg, regex, REG_EXTENDED);
regmatch_t rgm[1];
@(regexec(&reg, str, 1, rgm, 0) == 0);
@(rgm[0].rm_eo - rgm[0].rm_so == strlen(str));
printf("Yes\n");

printf("No\n");

@(~scanf("%s%s", regex, str));
regex_t reg;
regcomp(&reg, regex, REG_EXTENDED);
regmatch_t rgm[1];
@(regexec(&reg, str, 1, rgm, 0) == 0);
@(rgm[0].rm_eo - rgm[0].rm_so == strlen(str));
printf("Yes\n");

printf("No\n");

@(~scanf("%s%s", regex, str));
regex_t reg;
regcomp(&reg, regex, REG_EXTENDED);
regmatch_t rgm[1];
@(regexec(&reg, str, 1, rgm, 0) == 0);
@(rgm[0].rm_eo - rgm[0].rm_so == strlen(str));
printf("Yes\n");

printf("No\n");

@(!(~scanf("%s%s", regex, str)));

return 0;
