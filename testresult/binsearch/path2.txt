int arr[10];
int n;
int target;
int left = 0;
int right = n - 1;
@(left <= right);
int mid = left + (right - left) / 2;
@(!(arr[mid] == target));

@(arr[mid] < target);
left = mid + 1;


;
@(left <= right);
int mid = left + (right - left) / 2;
@(!(arr[mid] == target));

@(arr[mid] < target);
left = mid + 1;


;
@(!(left <= right));

return -1;
