long long n;
long long k;
long long *input;
long long *array;
long long i;
long long index;
long long value;
long long *array;
long long tmp = index;
long long index;
long long *array;
long long value = 0;
long long l;
long long r;
long long *array;
long long m,i,flag,tmpindex,tmpl,tmpr,tmpvalue;
long long sumvalue;
long long input[n],tarray[n+1];
i = 1;
@(i < n+1);
i++;
array[i] += input[i-1];
@(i + lowbits(i) <= n);
array[i+lowbits(i)] += array[i];

@(i < n+1);
i++;
array[i] += input[i-1];
@(i + lowbits(i) <= n);
array[i+lowbits(i)] += array[i];

@(i < n+1);
i++;
array[i] += input[i-1];
@(!(i + lowbits(i) <= n));
@(tmp <= n);
array[tmp] += value;
tmp += lowbits(tmp);
@(tmp <= n);
array[tmp] += value;
tmp += lowbits(tmp);
@(tmp <= n);
array[tmp] += value;
tmp += lowbits(tmp);
@(!(tmp <= n));

@(index);
value += array[index];
index -= lowbits(index);
@(index);
value += array[index];
index -= lowbits(index);
@(index);
value += array[index];
index -= lowbits(index);
@(!(index));

return value;
