int ne[1000100],len1,len2;
char a[1000100],b[1000100];
int j=-1,i=0;
int j=0,i=0;
int ans=0;
ne[0]=-1;
@(i<len2);
@(!(j==-1||b[i]==b[j]));

j=ne[j];

@(i<len2);
@(!(j==-1||b[i]==b[j]));

j=ne[j];

@(i<len2);
@(!(j==-1||b[i]==b[j]));

j=ne[j];

@(!(i<len2));

@(i<len1);
@(!(j==-1||a[i]==b[j]));

j=ne[j];

@(j==len2);
j=ne[j];
ans++;

@(i<len1);
@(j==-1||a[i]==b[j]);
++i;
++j;

@(!(j==len2));

@(i<len1);
@(!(j==-1||a[i]==b[j]));

j=ne[j];

@(!(j==len2));

@(!(i<len1));

printf("%d\n",ans);
