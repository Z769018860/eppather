int arr[10];
int size;
int count = 0;
int isPeak = 0;
int i = 1;
@(i < size - 1);
i = i + 1;
@(arr[i] > 0);
@(!(arr[i] > arr[i - 1]));


@(!(arr[i] < arr[i - 1]));


;

;
@(i < size - 1);
i = i + 1;
@(arr[i] > 0);
@(arr[i] > arr[i - 1]);
@(!(arr[i] > arr[i + 1]));


;

@(!(arr[i] < arr[i - 1]));


;

;
@(!(i < size - 1));
return count;
