long long n;
long long m;
long long P;
int sum = 1;
long long n, P;
@(m);
@(!(m & 1));

n = (n * n) % P;
m >>= 1;
@(m);
@(m & 1);
sum=(sum*n)%P;

n = (n * n) % P;
m >>= 1;
@(m);
@(m & 1);
sum=(sum*n)%P;

n = (n * n) % P;
m >>= 1;
@(!(m));

return sum;
