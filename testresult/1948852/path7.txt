long long x;
long long a;
long long p;
long long result = 1;
long long x, n, ai, p = 998244352;
@(a > 0);
@(!(a % 2 == 1));

a = a / 2;
x = (x * x) % p;
@(a > 0);
@(!(a % 2 == 1));

a = a / 2;
x = (x * x) % p;
@(a > 0);
@(!(a % 2 == 1));

a = a / 2;
x = (x * x) % p;
@(!(a > 0));

return result;
