int arr[5];
int size;
int count = 0;
@(!(size < 3));


int i = 1;
@(i < size - 1);
i = i + 1;
@(!(arr[i] > arr[i - 1]));

@(!(arr[i] < arr[i - 1]));



;
@(i < size - 1);
i = i + 1;
@(!(arr[i] > arr[i - 1]));

@(arr[i] < arr[i - 1]);
@(!(arr[i] < arr[i + 1]));


;


;
@(!(i < size - 1));
return count;
