char ch;
long long* x;
long long n;
long long m;
long long P;
int sum = 1;
long long n, P;
*x = 0;
ch = getchar();
@(ch < '0' || ch > '9');
ch = getchar();
;
@(ch < '0' || ch > '9');
ch = getchar();
;
@(ch < '0' || ch > '9');
ch = getchar();
;
@(!(ch < '0' || ch > '9'));
@(ch >= '0' && ch <= '9');
ch = getchar();
*x = (*x << 3) + (*x << 1) + (ch & 15);
@(ch >= '0' && ch <= '9');
ch = getchar();
*x = (*x << 3) + (*x << 1) + (ch & 15);
@(ch >= '0' && ch <= '9');
ch = getchar();
*x = (*x << 3) + (*x << 1) + (ch & 15);
@(!(ch >= '0' && ch <= '9'));
@(m);
@(!(m & 1));

n = (n * n) % P;
m >>= 1;
@(m);
@(!(m & 1));

n = (n * n) % P;
m >>= 1;
@(m);
@(m & 1);
sum=(sum*n)%P;

n = (n * n) % P;
m >>= 1;
@(!(m));

return sum;
