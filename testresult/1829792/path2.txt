typedef long long ll;
ll tree[1000010];
int s[1000010],n,q;
int i;
int i;
int x;
int x;
ll sums=0;
int i,j,a,b,c;
@(x>0);
sums+=tree[x];
x-=move(x);
@(x>0);
sums+=tree[x];
x-=move(x);
@(x>0);
sums+=tree[x];
x-=move(x);
@(!(x>0));

return sums;
