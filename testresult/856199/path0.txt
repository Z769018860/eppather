char regx[233], str[233];
@(~scanf("%s %s", regx, str));
regex_t reg;
regcomp(&reg, regx, REG_EXTENDED);
regmatch_t mm[1];
@(regexec(&reg, str, 1, mm, 0) != REG_NOMATCH);
@(mm[0].rm_eo - mm[0].rm_so == strlen(str));
puts("Yes");

puts("No");

@(~scanf("%s %s", regx, str));
regex_t reg;
regcomp(&reg, regx, REG_EXTENDED);
regmatch_t mm[1];
@(regexec(&reg, str, 1, mm, 0) != REG_NOMATCH);
@(mm[0].rm_eo - mm[0].rm_so == strlen(str));
puts("Yes");

puts("No");

@(~scanf("%s %s", regx, str));
regex_t reg;
regcomp(&reg, regx, REG_EXTENDED);
regmatch_t mm[1];
@(regexec(&reg, str, 1, mm, 0) != REG_NOMATCH);
@(mm[0].rm_eo - mm[0].rm_so == strlen(str));
puts("Yes");

puts("No");

@(!(~scanf("%s %s", regx, str)));

return 0;
