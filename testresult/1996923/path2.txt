long long n, m;
long long tree[10000][10000];
long long x;
long long x;
long long y;
long long val;
long long x;
long long y;
long long ans = 0;
long long x0;
long long y0;
long long x1;
long long y1;
long long i, j, x, y, a, b, c, d, k;
@(x >= 1);
long long ty = y;
@(ty >= 1);
ans += tree[x][ty];
ty -= lowbit(ty);
@(ty >= 1);
ans += tree[x][ty];
ty -= lowbit(ty);
@(ty >= 1);
ans += tree[x][ty];
ty -= lowbit(ty);
@(!(ty >= 1));

x -= lowbit(x);
@(x >= 1);
long long ty = y;
@(ty >= 1);
ans += tree[x][ty];
ty -= lowbit(ty);
@(ty >= 1);
ans += tree[x][ty];
ty -= lowbit(ty);
@(ty >= 1);
ans += tree[x][ty];
ty -= lowbit(ty);
@(!(ty >= 1));

x -= lowbit(x);
@(x >= 1);
long long ty = y;
@(ty >= 1);
ans += tree[x][ty];
ty -= lowbit(ty);
@(ty >= 1);
ans += tree[x][ty];
ty -= lowbit(ty);
@(ty >= 1);
ans += tree[x][ty];
ty -= lowbit(ty);
@(!(ty >= 1));

x -= lowbit(x);
@(!(x >= 1));

return ans;
