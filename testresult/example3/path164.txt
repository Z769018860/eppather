int arr[5];
int size;
int maxProd = arr[0], minProd = arr[0], result = arr[0], temp;
@(!(size <= 0));


int i = 1;
@(i < size);
i = i + 1;
@(!(arr[i] < 0));


@(!(arr[i] * maxProd > arr[i]));
maxProd = arr[i];

@(arr[i] * minProd < arr[i]);
minProd = arr[i] * minProd;

@(!(maxProd > result));


;
@(i < size);
i = i + 1;
@(!(arr[i] < 0));


@(!(arr[i] * maxProd > arr[i]));
maxProd = arr[i];

@(!(arr[i] * minProd < arr[i]));
minProd = arr[i];

@(!(maxProd > result));


;
@(!(i < size));
return result;
