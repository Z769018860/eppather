typedef unsigned long long ll;
ll base;
ll exp;
ll mod;
ll ans = 1;
ll n, p, i;
@(exp);
@(!(exp & 1));

base = (base * base) % mod;
exp >>= 1;
@(exp);
@(!(exp & 1));

base = (base * base) % mod;
exp >>= 1;
@(exp);
@(!(exp & 1));

base = (base * base) % mod;
exp >>= 1;
@(!(exp));

return ans;
