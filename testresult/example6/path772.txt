int arr[10];
int size;
int count = 0;
int i = 0;
@(i < size);
i = i + 1;
@(!(i % 2 == 0));


@(i % 2 != 0);
@(!(arr[i] % 2 != 0));


;

@(!(arr[i] > 0));


@(!(arr[i] < 0));


;
@(i < size);
i = i + 1;
@(!(i % 2 == 0));


@(i % 2 != 0);
@(arr[i] % 2 != 0);
count = count + 1;

;

@(!(arr[i] > 0));


@(arr[i] < 0);
@(!(i < size - 1 && arr[i] < arr[i + 1]));


;

;
@(!(i < size));
return count;
