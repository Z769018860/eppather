typedef long long ll;
ll t[1000005];
int q,n,b[1000005],a[1000005];
int x;
int pos;
int k;
int i;
int pos;
int i;
ll ans=0;
int judge,i;
scanf("%d%d",&n,&q);
i=1;
@(i<=n);
i++;
scanf("%d",&a[i]);
b[i]=a[i]-a[i-1];
add(i,b[i]);
@(i<=n);
i++;
scanf("%d",&a[i]);
b[i]=a[i]-a[i-1];
add(i,b[i]);
@(i<=n);
i++;
scanf("%d",&a[i]);
b[i]=a[i]-a[i-1];
add(i,b[i]);
@(!(i<=n));
@(q--);
scanf("%d",&judge);
@(judge==1);
int l,r,x;
scanf("%d%d%d",&l,&r,&x);
add(l,x);
add(r+1,-x);

@(q--);
scanf("%d",&judge);
@(judge==1);
int l,r,x;
scanf("%d%d%d",&l,&r,&x);
add(l,x);
add(r+1,-x);

@(q--);
scanf("%d",&judge);
@(!(judge==1));
int x;
scanf("%d",&x);
printf("%lld\n",ask(x));

@(!(q--));

return 0;
