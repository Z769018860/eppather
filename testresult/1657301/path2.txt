typedef long long ll;
ll c[1000010];
int n, q;
int x;
int i;
ll a;
int x;
ll ans = 0;
int i, a, b = 0;
@(x > 0);
ans = ans + c[x];
x = x - lowbit(x);
@(x > 0);
ans = ans + c[x];
x = x - lowbit(x);
@(x > 0);
ans = ans + c[x];
x = x - lowbit(x);
@(!(x > 0));

return ans;
