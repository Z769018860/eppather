int *string;
int length;
int handler;
int *user;
int mem = 0;
@(string);
mem = mem + string[0];
@(!(length > 0));
mem = mem - length;
@(!(handler > 0));
mem = mem - handler;
@(user);
mem = mem + user[0];
return 0;
