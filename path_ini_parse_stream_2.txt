int reader;
int *stream;
int handler;
int *user;
int mem = 0;
@(reader > 0);
mem = mem + reader;
@(stream);
mem = mem + stream[0];
@(!(handler > 0));
mem = mem - handler;
@(user);
mem = mem + user[0];
return 0;
